[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scene-model"
dynamic = ["version"]
description = "Scene modeling and point-source extraction for SNIFS IFU data"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE", "THIRD_PARTY_LICENSES/*"]
authors = [
    { name = "Kyle Boone", email = "kboone@berkeley.edu" },
]
dependencies = [
    "astropy",
    "jax==0.10.2",
    "matplotlib",
    "numpy",
    "scipy",
]

[project.scripts]
extract-star2 = "scene_model.cli:main"

[project.optional-dependencies]
cuda13 = [
    "jax[cuda13]==0.10.2; sys_platform == 'linux' and platform_machine == 'x86_64'",
]
test = ["pytest>=8"]

[tool.setuptools]
packages = [
    "scene_model",
    "scene_model._compat",
    "scripts",
]

[tool.setuptools.package-data]
"scene_model._compat" = ["NOTICE.md"]

[tool.setuptools.dynamic]
version = { attr = "scene_model.config.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]
